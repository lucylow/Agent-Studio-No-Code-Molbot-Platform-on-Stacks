import Simulator from "./Simulator";

// "Deploy" is currently mapped to the existing x402/Stx simulation page.
// This keeps the new navigation functional until a dedicated deploy UI lands.
const Deploy = () => <Simulator />;

export default Deploy;

